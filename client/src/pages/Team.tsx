import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { Save, UserCog, UserPlus, UsersRound } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";

type TeamPerson = { id: number; name: string | null; email: string | null; role: "admin" | "supervisor" | "user"; store: string | null; displayName: string | null };
type NewUser = { name: string; email: string; password: string; role: "admin" | "supervisor" | "user"; pdvIds: number[] };
const emptyUser: NewUser = { name: "", email: "", password: "", role: "user", pdvIds: [] };

export default function Team() {
  const team = trpc.team.list.useQuery();
  const pdvs = trpc.pdvs.list.useQuery({ includeInactive: true });
  const utils = trpc.useUtils();
  const [open, setOpen] = useState(false);
  const [newUser, setNewUser] = useState<NewUser>(emptyUser);
  const [drafts, setDrafts] = useState<Record<number, { store: string; displayName: string }>>({});
  const assign = trpc.team.assign.useMutation({ onSuccess: () => { toast.success("Vínculo atualizado"); utils.team.list.invalidate(); }, onError: (error) => toast.error(error.message) });
  const create = trpc.users.create.useMutation({ onSuccess: () => { toast.success("Usuário criado com sucesso"); setOpen(false); setNewUser(emptyUser); utils.team.list.invalidate(); }, onError: (error) => toast.error(error.message) });
  const activePdvs = (pdvs.data ?? []).filter((pdv) => pdv.isActive);
  const togglePdv = (pdvId: number, checked: boolean) => setNewUser((current) => ({ ...current, pdvIds: checked ? [...current.pdvIds, pdvId] : current.pdvIds.filter((id) => id !== pdvId) }));
  const saveLegacyProfile = (person: TeamPerson) => { const draft = drafts[person.id] ?? { store: person.store ?? "", displayName: person.displayName ?? person.name ?? "" }; assign.mutate({ userId: person.id, ...draft }); };
  const updateDraft = (person: TeamPerson, field: "store" | "displayName", value: string) => setDrafts((current) => ({ ...current, [person.id]: { ...(current[person.id] ?? { store: person.store ?? "", displayName: person.displayName ?? person.name ?? "" }), [field]: value } }));
  const submit = (event: FormEvent) => { event.preventDefault(); create.mutate(newUser); };
  return <DashboardLayout><div className="mx-auto max-w-[1050px] space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="mb-2 text-[11px] font-semibold uppercase tracking-[.18em] text-[#6da768]">Administração</div><h1 className="text-3xl font-semibold tracking-[-.04em] text-[#102b35]">Usuários</h1><p className="mt-2 text-sm text-muted-foreground">Crie acessos, defina perfis e vincule os PDVs autorizados.</p></div><Button onClick={() => setOpen(true)} className="h-10 rounded-xl bg-[#102b35] hover:bg-[#173b47]"><UserPlus className="mr-2 h-4 w-4" /> Incluir usuário</Button></div><Card className="border-0 bg-white/80 shadow-[0_10px_35px_-25px_rgba(16,43,53,.4)]"><CardHeader className="border-b border-[#e8eeed]"><CardTitle className="flex items-center gap-2 text-lg"><UsersRound className="h-5 w-5 text-[#6da768]" /> Usuários cadastrados</CardTitle></CardHeader><CardContent className="p-5">{team.isLoading ? <p className="p-8 text-center text-sm text-muted-foreground">Carregando usuários...</p> : team.data?.length === 0 ? <p className="rounded-2xl bg-[#f5faf5] p-8 text-center text-sm text-muted-foreground">Use <strong>Incluir usuário</strong> para criar o primeiro acesso.</p> : <div className="space-y-3">{team.data?.map((person: TeamPerson) => { const draft = drafts[person.id] ?? { store: person.store ?? "", displayName: person.displayName ?? person.name ?? "" }; return <div key={person.id} className="grid gap-4 rounded-2xl border border-[#e5eee7] bg-[#fbfdfb] p-4 lg:grid-cols-[1.1fr_1fr_1fr_auto]"><div><p className="font-semibold text-[#1d3d45]">{person.name || "Usuário"}</p><p className="text-xs text-muted-foreground">{person.email}</p><Badge variant="outline" className="mt-2 rounded-full">{person.role === "admin" ? "Administrador" : person.role === "supervisor" ? "Supervisor" : "Vendedor"}</Badge></div><Input value={draft.displayName} aria-label="Nome na carteira" onChange={(event) => updateDraft(person, "displayName", event.target.value)} /><Select value={draft.store || "unassigned"} onValueChange={(value) => updateDraft(person, "store", value === "unassigned" ? "" : value)}><SelectTrigger><SelectValue placeholder="PDV" /></SelectTrigger><SelectContent><SelectItem value="unassigned">Sem PDV</SelectItem>{activePdvs.map((pdv) => <SelectItem key={pdv.id} value={pdv.name}>{pdv.name}</SelectItem>)}</SelectContent></Select><Button disabled={!draft.store || !draft.displayName || assign.isPending} onClick={() => saveLegacyProfile(person)}><Save className="mr-2 h-4 w-4" /> Salvar</Button></div>; })}</div>}</CardContent></Card><Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-xl"><DialogHeader><DialogTitle className="flex items-center gap-2"><UserCog className="h-5 w-5 text-[#6da768]" /> Incluir usuário</DialogTitle><DialogDescription>A senha é guardada de forma protegida e nunca em texto puro.</DialogDescription></DialogHeader><form onSubmit={submit} className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-1.5 sm:col-span-2"><label className="text-sm font-medium">Nome completo</label><Input required value={newUser.name} onChange={(event) => setNewUser({ ...newUser, name: event.target.value })} /></div><div className="space-y-1.5"><label className="text-sm font-medium">E-mail</label><Input required type="email" value={newUser.email} onChange={(event) => setNewUser({ ...newUser, email: event.target.value })} /></div><div className="space-y-1.5"><label className="text-sm font-medium">Senha inicial</label><Input required type="password" minLength={8} value={newUser.password} onChange={(event) => setNewUser({ ...newUser, password: event.target.value })} /></div><div className="space-y-1.5 sm:col-span-2"><label className="text-sm font-medium">Perfil</label><Select value={newUser.role} onValueChange={(role: NewUser["role"]) => setNewUser({ ...newUser, role })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="user">Vendedor</SelectItem><SelectItem value="supervisor">Supervisor</SelectItem><SelectItem value="admin">Administrador</SelectItem></SelectContent></Select></div></div><div className="space-y-2"><label className="text-sm font-medium">PDVs autorizados {newUser.role === "user" && <span className="text-destructive">*</span>}</label><div className="grid gap-2 rounded-xl border p-3 sm:grid-cols-2">{activePdvs.map((pdv) => <label key={pdv.id} className="flex items-center gap-2 text-sm"><Checkbox checked={newUser.pdvIds.includes(pdv.id)} onCheckedChange={(checked) => togglePdv(pdv.id, checked === true)} />{pdv.name}</label>)}{!activePdvs.length && <p className="text-sm text-muted-foreground">Cadastre um PDV antes de criar vendedores.</p>}</div></div><DialogFooter><Button type="button" variant="outline" onClick={() => setOpen(false)}>Cancelar</Button><Button type="submit" disabled={create.isPending || (newUser.role === "user" && !newUser.pdvIds.length)}>{create.isPending ? "Criando..." : "Criar usuário"}</Button></DialogFooter></form></DialogContent></Dialog></div></DashboardLayout>;
}
